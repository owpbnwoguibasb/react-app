version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Which Node - $(node -v)"
        - yarn install
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
